# iTu Web

Standalone Vite React web application for iTu.

## Setup

```bash
yarn install
cp .env.example .env
```

## Development

```bash
yarn dev
```

## Verification

```bash
yarn typecheck
yarn test
yarn build
```

Set `VITE_API_BASE_URL` to the independently deployed iTu API.
