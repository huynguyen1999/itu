VITE_API_BASE_URL="http://localhost:3000"
VITE_API_TIMEOUT_MS="30000"
# Enable the unified POST /sync (push+pull) endpoint. Defaults to the split
# /sync/mutations + /sync/changes flow when unset or "false".
VITE_USE_UNIFIED_SYNC="false"
